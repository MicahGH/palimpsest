[project]
name = "palimpsest"
version = "0.1.0"
description = "AI-powered software archaeology for uncovering the history and rationale behind code."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
